/**
 * SkillsSection — Phase 1 stub. Built in Phase 6 (tech radar + animated bars).
 */
export default function SkillsSection() {
  return (
    <section id="skills" className="section section--skills" aria-label="Skills">
      {/* Phase 6: interactive tech radar + animated skill bars */}
    </section>
  );
}
