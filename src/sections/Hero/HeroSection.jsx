/**
 * HeroSection — Phase 1 stub. Cinematic entry built in Phase 5.
 * Hosts the 3D <HeroScene /> plus the name animation, particle burst & scroll cue.
 */
export default function HeroSection() {
  return (
    <section id="hero" className="section section--hero" aria-label="Intro">
      {/* Phase 5: HeroScene canvas + animated name + scroll cue */}
    </section>
  );
}
