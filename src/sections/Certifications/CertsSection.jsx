/**
 * CertsSection — Phase 1 stub. Built in Phase 8 (certificate cards + education).
 */
export default function CertsSection() {
  return (
    <section id="certifications" className="section section--certs" aria-label="Certifications">
      {/* Phase 8: certificate cards + education */}
    </section>
  );
}
