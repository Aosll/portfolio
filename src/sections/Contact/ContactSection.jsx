/**
 * ContactSection — Phase 1 stub. Built in Phase 9 (contact form + footer).
 */
export default function ContactSection() {
  return (
    <section id="contact" className="section section--contact" aria-label="Contact">
      {/* Phase 9: contact form + social links + footer */}
    </section>
  );
}
